# Production values.
# Prefer setting all env vars in Vercel Project Settings → Environment Variables.

# Frontend (optional): explicit redirect URL if you want to control it at build-time.
# VITE_STRAVA_REDIRECT_URI=https://tetris-strava.vercel.app/strava/callback

# Server-only secrets (do NOT prefix with VITE_). Used by serverless functions.
# These should be set in Vercel, not committed.
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

