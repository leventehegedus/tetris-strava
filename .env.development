# Local development values
# Frontend-exposed (safe):
VITE_STRAVA_CLIENT_ID=194362
# Optional explicit redirect (use exact URL when testing)
# VITE_STRAVA_REDIRECT_URI=http://localhost:5173/strava/callback

# Server-only secrets (do NOT prefix with VITE_):
STRAVA_CLIENT_ID=194362
STRAVA_CLIENT_SECRET=
